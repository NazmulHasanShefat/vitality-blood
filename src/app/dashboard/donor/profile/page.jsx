import React from 'react';
import ProfileCard from './ProfileCard';

const ProfilePage = () => {
    return (
        <section>
            <ProfileCard />
        </section>
    );
};

export default ProfilePage;